import React from 'react';
import TodosContainer from '../containers/TodosContainer';

const TodosList = () => {
    return (
        <div>
            <TodosContainer />
        </div>
    );
};

export default TodosList;
